# Node modules (backend)
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# ──────────────────────────────────────
# Design-sync generated / machine state
# ──────────────────────────────────────
# Staged converter scripts + their deps (recreated per clone by re-copying from the skill)
.ds-sync/

# Generated bundle output (rebuilt by running package-build.mjs)
ds-bundle/

# Machine-local verification state (carry-forward comes from uploaded _ds_sync.json)
.design-sync/.cache/

# Subagent scratch (merged into NOTES.md before upload)
.design-sync/learnings/

# Fork symlink (recreated with: ln -sfn ../.ds-sync/node_modules .design-sync/node_modules)
.design-sync/node_modules
