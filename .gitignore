# Node modules (backend)
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# ──────────────────────────────────────
# Internal engineering docs & AI tooling (kept local, not published)
# ──────────────────────────────────────
CLAUDE.md
docs/
.claude/

# ──────────────────────────────────────
# Design-sync generated / machine state
# ──────────────────────────────────────
# Staged converter scripts + their deps (recreated per clone by re-copying from the skill)
.ds-sync/

# Generated bundle output (rebuilt by running package-build.mjs)
ds-bundle/

# Design-sync workspace (machine/tooling state, not project source)
.design-sync/

# ──────────────────────────────────────
# Local API & testing tools
# ──────────────────────────────────────
# Postman collection (kept local — may contain auth tokens or internal request details)
GoodsGo.postman_collection.json
*.postman_collection.json
